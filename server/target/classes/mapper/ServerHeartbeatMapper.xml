<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.safetyfire.monitor.mapper.ServerHeartbeatMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO server_heartbeat (ip, heartbeat_time, created_at, updated_at)
        VALUES (#{ip}, #{heartbeatTime}, NOW(3), NOW(3))
    </insert>

</mapper>

