<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.safetyfire.monitor.mapper.AdminMetaMapper">

    <select id="listAllPermissionKeys" resultType="string">
        SELECT perm_key FROM permission ORDER BY perm_key ASC
    </select>

    <select id="listAllMenus" resultType="com.safetyfire.monitor.domain.vo.MenuVO">
        SELECT id,
               parent_id AS parentId,
               menu_key  AS menuKey,
               menu_name AS menuName,
               path      AS path,
               icon      AS icon,
               sort_no   AS sortNo
        FROM menu
        WHERE enabled = 1
        ORDER BY sort_no ASC, id ASC
    </select>

</mapper>

