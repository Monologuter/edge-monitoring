<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.safetyfire.monitor.mapper.RoleMapper">

    <insert id="upsertAdminRole">
        INSERT INTO role (role_key, role_name, created_at, updated_at)
        VALUES ('ADMIN', '系统管理员', NOW(3), NOW(3))
        ON DUPLICATE KEY UPDATE role_name=VALUES(role_name), updated_at=NOW(3)
    </insert>

    <insert id="upsertRole">
        INSERT INTO role (role_key, role_name, created_at, updated_at)
        VALUES (#{roleKey}, #{roleName}, NOW(3), NOW(3))
        ON DUPLICATE KEY UPDATE role_name=VALUES(role_name), updated_at=NOW(3)
    </insert>

    <select id="findIdByRoleKey" resultType="long">
        SELECT id FROM role WHERE role_key = #{roleKey} LIMIT 1
    </select>

</mapper>
