<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.safetyfire.monitor.mapper.DeviceReadingQueryMapper">

    <select id="listHourly" resultType="com.safetyfire.monitor.domain.vo.DeviceReadingHourVO">
        SELECT device_code AS deviceCode,
               hour_start  AS hourStart,
               avg_value   AS avgValue,
               min_value   AS minValue,
               max_value   AS maxValue,
               samples     AS samples
        FROM device_reading_hour
        WHERE device_code = #{deviceCode}
          AND hour_start BETWEEN #{start} AND #{end}
        ORDER BY hour_start ASC
    </select>

</mapper>

