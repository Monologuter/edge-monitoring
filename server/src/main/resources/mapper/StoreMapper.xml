<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.safetyfire.monitor.mapper.StoreMapper">

    <resultMap id="StoreMap" type="com.safetyfire.monitor.domain.entity.StoreEntity">
        <id column="id" property="id"/>
        <result column="company_code" property="companyCode"/>
        <result column="store_num" property="storeNum"/>
        <result column="store_name" property="storeName"/>
        <result column="area" property="area"/>
        <result column="danger_level" property="dangerLevel"/>
        <result column="quota_dosage" property="quotaDosage"/>
        <result column="quota_people" property="quotaPeople"/>
    </resultMap>

    <select id="findById" resultMap="StoreMap">
        SELECT id, company_code, store_num, store_name, area, danger_level, quota_dosage, quota_people
        FROM store
        WHERE id = #{id}
        LIMIT 1
    </select>

    <select id="findByStoreNum" resultMap="StoreMap">
        SELECT id, company_code, store_num, store_name, area, danger_level, quota_dosage, quota_people
        FROM store
        WHERE store_num = #{storeNum}
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO store (company_code, store_num, store_name, area, danger_level, quota_dosage, quota_people, created_at, updated_at)
        VALUES (#{companyCode}, #{storeNum}, #{storeName}, #{area}, #{dangerLevel}, #{quotaDosage}, #{quotaPeople}, NOW(3), NOW(3))
    </insert>

    <update id="update">
        UPDATE store
        SET company_code = #{companyCode},
            store_num = #{storeNum},
            store_name = #{storeName},
            area = #{area},
            danger_level = #{dangerLevel},
            quota_dosage = #{quotaDosage},
            quota_people = #{quotaPeople},
            updated_at = NOW(3)
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM store WHERE id = #{id}
    </delete>

    <select id="list" resultMap="StoreMap">
        SELECT id, company_code, store_num, store_name, area, danger_level, quota_dosage, quota_people
        FROM store
        <where>
            <if test="keyword != null and keyword != ''">
                (store_num LIKE CONCAT('%', #{keyword}, '%')
                    OR store_name LIKE CONCAT('%', #{keyword}, '%')
                    OR company_code LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="companyCodes != null and companyCodes.size() > 0">
                <if test="keyword != null and keyword != ''">AND</if>
                company_code IN
                <foreach collection="companyCodes" item="c" open="(" separator="," close=")">
                    #{c}
                </foreach>
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(1)
        FROM store
        <where>
            <if test="keyword != null and keyword != ''">
                (store_num LIKE CONCAT('%', #{keyword}, '%')
                    OR store_name LIKE CONCAT('%', #{keyword}, '%')
                    OR company_code LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="companyCodes != null and companyCodes.size() > 0">
                <if test="keyword != null and keyword != ''">AND</if>
                company_code IN
                <foreach collection="companyCodes" item="c" open="(" separator="," close=")">
                    #{c}
                </foreach>
            </if>
        </where>
    </select>

</mapper>
