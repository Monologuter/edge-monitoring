<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.safetyfire.monitor.mapper.AlarmActionLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO alarm_action_log (alarm_id, action, operator, remark, action_time, created_at, updated_at)
        VALUES (#{alarmId}, #{action}, #{operator}, #{remark}, #{actionTime}, NOW(3), NOW(3))
    </insert>

</mapper>

