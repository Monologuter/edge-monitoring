<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.safetyfire.monitor.mapper.AiBoxAlarmMapper">

    <resultMap id="AiBoxAlarmMap" type="com.safetyfire.monitor.domain.entity.AiBoxAlarmEntity">
        <id column="id" property="id"/>
        <result column="company_code" property="companyCode"/>
        <result column="alarm_type" property="alarmType"/>
        <result column="alarm_status" property="alarmStatus"/>
        <result column="warning_time" property="warningTime"/>
        <result column="camera_code" property="cameraCode"/>
        <result column="area_code" property="areaCode"/>
        <result column="height" property="height"/>
        <result column="weight" property="weight"/>
        <result column="description" property="description"/>
        <result column="image_file_id" property="imageFileId"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_box_alarm (company_code, alarm_type, alarm_status, warning_time, camera_code, area_code, height, weight, description, image_file_id, created_at, updated_at)
        VALUES (#{companyCode}, #{alarmType}, #{alarmStatus}, #{warningTime}, #{cameraCode}, #{areaCode}, #{height}, #{weight}, #{description}, #{imageFileId}, NOW(3), NOW(3))
    </insert>

    <select id="listByCompanyCodes" resultMap="AiBoxAlarmMap">
        SELECT id, company_code, alarm_type, alarm_status, warning_time, camera_code, area_code, height, weight, description, image_file_id
        FROM ai_box_alarm
        <where>
            <if test="companyCodes != null and companyCodes.size() > 0">
                company_code IN
                <foreach collection="companyCodes" item="c" open="(" separator="," close=")">
                    #{c}
                </foreach>
            </if>
            <if test="alarmType != null and alarmType != ''">
                AND alarm_type = #{alarmType}
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countByCompanyCodes" resultType="long">
        SELECT COUNT(1)
        FROM ai_box_alarm
        <where>
            <if test="companyCodes != null and companyCodes.size() > 0">
                company_code IN
                <foreach collection="companyCodes" item="c" open="(" separator="," close=")">
                    #{c}
                </foreach>
            </if>
            <if test="alarmType != null and alarmType != ''">
                AND alarm_type = #{alarmType}
            </if>
        </where>
    </select>

</mapper>
