<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.safetyfire.monitor.mapper.CarMapper">

    <resultMap id="CarMap" type="com.safetyfire.monitor.domain.entity.CarEntity">
        <id column="id" property="id"/>
        <result column="company_code" property="companyCode"/>
        <result column="license_plate_number" property="licensePlateNumber"/>
        <result column="driver_name" property="driverName"/>
        <result column="driver_phone" property="driverPhone"/>
        <result column="valid_start" property="validStart"/>
        <result column="valid_end" property="validEnd"/>
        <result column="license_file_id" property="licenseFileId"/>
        <result column="car_type" property="carType"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findById" resultMap="CarMap">
        SELECT id, company_code, license_plate_number, driver_name, driver_phone, valid_start, valid_end, license_file_id, car_type, updated_at
        FROM car
        WHERE id = #{id}
        LIMIT 1
    </select>

    <select id="findByPlate" resultMap="CarMap">
        SELECT id, company_code, license_plate_number, driver_name, driver_phone, valid_start, valid_end, license_file_id, car_type, updated_at
        FROM car
        WHERE license_plate_number = #{plate}
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO car (company_code, license_plate_number, driver_name, driver_phone, valid_start, valid_end, license_file_id,
                         car_type, created_at, updated_at)
        VALUES (#{companyCode}, #{licensePlateNumber}, #{driverName}, #{driverPhone}, #{validStart}, #{validEnd}, #{licenseFileId},
                #{carType}, NOW(3), NOW(3))
    </insert>

    <update id="update">
        UPDATE car
        SET company_code = #{companyCode},
            license_plate_number = #{licensePlateNumber},
            driver_name = #{driverName},
            driver_phone = #{driverPhone},
            valid_start = #{validStart},
            valid_end = #{validEnd},
            license_file_id = #{licenseFileId},
            car_type = #{carType},
            updated_at = NOW(3)
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM car WHERE id = #{id}
    </delete>

    <select id="list" resultMap="CarMap">
        SELECT id, company_code, license_plate_number, driver_name, driver_phone, valid_start, valid_end, license_file_id, car_type, updated_at
        FROM car
        <where>
            <if test="keyword != null and keyword != ''">
                (license_plate_number LIKE CONCAT('%', #{keyword}, '%')
                    OR company_code LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="companyCodes != null and companyCodes.size() > 0">
                <if test="keyword != null and keyword != ''">AND</if>
                company_code IN
                <foreach collection="companyCodes" item="c" open="(" separator="," close=")">
                    #{c}
                </foreach>
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(1)
        FROM car
        <where>
            <if test="keyword != null and keyword != ''">
                (license_plate_number LIKE CONCAT('%', #{keyword}, '%')
                    OR company_code LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="companyCodes != null and companyCodes.size() > 0">
                <if test="keyword != null and keyword != ''">AND</if>
                company_code IN
                <foreach collection="companyCodes" item="c" open="(" separator="," close=")">
                    #{c}
                </foreach>
            </if>
        </where>
    </select>

</mapper>
