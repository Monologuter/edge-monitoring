<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.safetyfire.monitor.mapper.PermissionMapper">

    <select id="listPermKeysByUserId" resultType="string">
        SELECT DISTINCT p.perm_key
        FROM user_role ur
                 INNER JOIN role_permission rp ON rp.role_id = ur.role_id
                 INNER JOIN permission p ON p.id = rp.perm_id
        WHERE ur.user_id = #{userId}
        ORDER BY p.perm_key ASC
    </select>

</mapper>

