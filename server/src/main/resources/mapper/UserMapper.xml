<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.safetyfire.monitor.mapper.UserMapper">

    <resultMap id="UserEntityMap" type="com.safetyfire.monitor.domain.entity.UserEntity">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="display_name" property="displayName"/>
        <result column="enabled" property="enabled"/>
    </resultMap>

    <select id="findByUsername" resultMap="UserEntityMap">
        SELECT id, username, password_hash, display_name, enabled
        FROM user_account
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="findById" resultMap="UserEntityMap">
        SELECT id, username, password_hash, display_name, enabled
        FROM user_account
        WHERE id = #{id}
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_account (username, password_hash, display_name, enabled, created_at, updated_at)
        VALUES (#{username}, #{passwordHash}, #{displayName}, #{enabled}, NOW(3), NOW(3))
    </insert>

    <select id="listRolesByUserId" resultType="string">
        SELECT r.role_key
        FROM user_role ur
                 INNER JOIN role r ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
        ORDER BY r.id DESC
    </select>

    <update id="updatePassword">
        UPDATE user_account
        SET password_hash = #{passwordHash},
            updated_at = NOW(3)
        WHERE id = #{id}
    </update>

    <select id="listUsers" resultMap="UserEntityMap">
        SELECT id, username, password_hash, display_name, enabled
        FROM user_account
        <where>
            <if test="keyword != null and keyword != ''">
                (username LIKE CONCAT('%', #{keyword}, '%')
                    OR display_name LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countUsers" resultType="long">
        SELECT COUNT(1)
        FROM user_account
        <where>
            <if test="keyword != null and keyword != ''">
                (username LIKE CONCAT('%', #{keyword}, '%')
                    OR display_name LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </select>

    <delete id="deleteById">
        DELETE FROM user_account WHERE id = #{id}
    </delete>

    <update id="updateBasic">
        UPDATE user_account
        SET display_name = #{displayName},
            enabled = #{enabled},
            updated_at = NOW(3)
        WHERE id = #{id}
    </update>

</mapper>
