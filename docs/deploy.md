# 部署与回滚（本地/测试环境）

## 依赖

- JDK 21
- MySQL 8
- Redis 7

## 启动顺序

1. 启动 MySQL/Redis（可用 `docker-compose.yml`）
2. 启动后端 `server`
3. 启动前端 `web`

## 回滚策略（建议）

- 数据库变更：Flyway 版本化脚本；上线前备份；回滚优先“前滚修复”
- 应用回滚：保留上一版本制品/镜像；灰度切流；快速回退

